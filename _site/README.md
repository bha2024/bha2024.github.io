# BioHackAcademy.github.io
Waag's BioHack Academy Syllabus

```
rvm use ruby-2.5.3
gem install jekyll -v 3.9.2
jekyll build -d public
# for testing
public$ ruby -run -ehttpd . -p8000

```

Temporarily no CI, deploy public dir manually. Be aware.. the site is 2GB!
